# Resume Webpage (HTML Only)

This project is a simple **resume webpage built using only HTML**, without any CSS or JavaScript.  
It is created as part of an assignment to demonstrate understanding of **HTML structure, semantic tags, tables, lists, and basic webpage layout** using default browser styling.

---

## 🛠️ Technologies Used

- HTML5

---

## 🚀 Setup Instructions

Follow these steps to run the project locally:

1. Clone the repository or download the files
2. Make sure `index.html` is present in the project folder
3. Open the file using any of the following methods:
   - Double-click `index.html`
   - Right-click → **Open with browser**
   - OR open using **Live Server** in VS Code

